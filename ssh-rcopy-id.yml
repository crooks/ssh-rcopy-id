---
loglevel: info
authentication:
  ssh_dir:
  ssh_private_keys:
    <keyfilename>: root

source:
  ssh_keys_dir: ~/pubkeys
destination:
  base_homedir: /home
  ssh_dir: .ssh
  authorized_keys: authorized_keys
  hosts:
    - notarealone
    - anotherfake
